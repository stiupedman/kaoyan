<Window x:Class="KaoyanFocus.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="考研自律神器" Width="760" Height="680" MinWidth="620" MinHeight="560"
 Background="#EEF4FF" WindowStartupLocation="CenterScreen">
  <ScrollViewer>
    <Border Margin="32" Padding="28" Background="White" CornerRadius="18">
      <StackPanel>
        <TextBlock Text="距离考研还有" Foreground="#667085"/>
        <TextBlock x:Name="DaysText" FontSize="52" FontWeight="Bold" Foreground="{StaticResource Accent}"/>
        <TextBlock x:Name="DateSourceText" Foreground="#667085" Margin="0,0,0,22"/>
        <TextBlock Text="今日任务" FontSize="22" FontWeight="SemiBold"/>
        <TextBlock Text="填写任务名称和计划时长（整数分钟，至少 1 分钟）" Foreground="#667085" Margin="0,4,0,0"/>
        <ItemsControl x:Name="TaskList" Margin="0,12,0,8">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition/>
                  <ColumnDefinition Width="90"/>
                  <ColumnDefinition Width="48"/>
                  <ColumnDefinition Width="48"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" Padding="10"/>
                <TextBox Grid.Column="1" Text="{Binding TargetMinutesText, UpdateSourceTrigger=PropertyChanged}" Padding="10"/>
                <TextBlock Grid.Column="2" Text="分钟" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <Button Grid.Column="3" Content="删" Tag="{Binding}" Click="DeleteTask_Click" Margin="8,0,0,0"/>
              </Grid>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button x:Name="AddTaskButton" Content="＋ 添加任务" Click="AddTask_Click" Padding="10" HorizontalAlignment="Left"/>
        <Border Margin="0,20,0,0" Padding="16" Background="#F5F8FF" CornerRadius="10">
          <StackPanel>
            <CheckBox x:Name="StrictModeCheckBox" Content="启用家庭版强化模式" FontWeight="SemiBold"/>
            <TextBlock Margin="24,5,0,10" Foreground="#667085" TextWrapping="Wrap"
                       Text="覆盖所有显示器、隐藏任务栏、限制系统切换键，并最小化黑名单程序；Ctrl+Alt+Del 始终保留。"/>
            <StackPanel x:Name="StrictOptionsPanel" Margin="24,0,0,0">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="80"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="离座暂停：" VerticalAlignment="Center"/>
                <TextBox x:Name="IdleMinutesTextBox" Grid.Column="1" Padding="8"/>
                <TextBlock Grid.Column="2" Text="分钟无操作后暂停计时（1–120）" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="#667085"/>
              </Grid>
              <TextBlock Text="进程黑名单（逗号分隔，仅最小化，不结束进程）" Margin="0,12,0,5"/>
              <TextBox x:Name="BlockedProcessesTextBox" Padding="8" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="54"/>
            </StackPanel>
          </StackPanel>
        </Border>
        <TextBlock x:Name="EmergencyText" Margin="0,18,0,10" Foreground="#667085"/>
        <TextBlock x:Name="ErrorText" Foreground="#C62828" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <Button x:Name="PrimaryButton" Content="开始今日学习" Click="PrimaryButton_Click"
                Padding="16" Background="{StaticResource Accent}" Foreground="White" FontWeight="SemiBold"/>
        <StackPanel x:Name="ManualDatePanel" Visibility="Collapsed" Margin="0,16,0,0">
          <TextBlock Text="暂未获取官方日期，请设置备用日期"/>
          <DatePicker x:Name="ManualDatePicker" Margin="0,8,0,0" SelectedDateChanged="ManualDateChanged"/>
        </StackPanel>
      </StackPanel>
    </Border>
  </ScrollViewer>
</Window>
